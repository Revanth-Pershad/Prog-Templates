// We can build a binary tree if atleast 2 traversals are given and one of them is inOrder Traversal.
// We write a recursive solution that finds the root from the traversals then breaks the traversals left
// and right subtree and then recursively solves for the both subtrees
